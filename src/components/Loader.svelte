<div class="loader">
	Loading
</div>

<style>
	@keyframes pulsing {
		50% { opacity: 0.6; }
	}

	.loader {
		margin-top: 8rem;
		color: var(--color-secondary);
		text-align: center;
		text-transform: uppercase;
		animation: pulsing 0.8s infinite;
	}
</style>
